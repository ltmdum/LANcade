{
  "name": "lancade",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/extract-word-lists.js && concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "node scripts/extract-word-lists.js && node scripts/validate-games-config.js && npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "test": "npm run test -w backend && npm run test -w frontend",
    "validate-config": "node scripts/validate-games-config.js"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.2",
    "word-list": "^4.1.0"
  }
}
